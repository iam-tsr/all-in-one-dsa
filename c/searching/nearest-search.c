#include <stdio.h>

int numType(int x){
    return x<0 ? -x : x;
}

int findClosestNumber(int* nums, int numsSize){
    int type = numType(nums);
}

int main() {

    // int nums[] = {1, 2, 3, 4, 5};

    // int x = numType(-1);
    // printf("%d", x);
    st *s;
    int num;

    s->num = 3;
    printf("%d", num);
    return 0;
}